# Bloomium Environment Configuration
# Copy this to .env.local and fill in your values

# Mode: "local" or "cloud"
MODE=local

# Storage
STORAGE_PATH=./local-data

# API Configuration
API_PORT=3001
API_URL=http://localhost:3001

# Web Configuration
WEB_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:3001

# Google Cloud (for production - fill these in when deploying)
# GCP_PROJECT_ID=bloomium-prod
# GCP_REGION=us-central1
# GCS_BUCKET=bloomium-tiles
